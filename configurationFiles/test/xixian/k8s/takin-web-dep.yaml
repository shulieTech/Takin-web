apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: shulie-takin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - name: web
        image: harbor.dcos.xixian.unicom.local/skyeye-platform/stresstest/web:2.0.0
        imagePullPolicy: IfNotPresent
        lifecycle:
          postStart:
            exec:
              command:
                - "/bin/sh"
                - "-c"
                - "mkdir -p /data/nfs_dir && mount -o rw,intr,soft,timeo=30,retry=3,nolock -t nfs 10.125.86.233:/data/share /data/nfs_dir"
        securityContext:
          capabilities:
            add: ["SYS_ADMIN"]
        resources:
          requests:
            memory: 4G
          limits:
            memory: 4G
        command: ["./start.sh" ]
