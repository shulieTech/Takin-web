# Service配置-暴露服务
apiVersion: v1
kind: Service
metadata:
  name: takin-web-svc
  namespace: shulie-takin
spec:
#  type: NodePort
  selector:
    app: takin-web
  ports:
    - name: takin-web
      port: 10008
      targetPort: 10008