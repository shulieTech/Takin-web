# Service配置-暴露服务
apiVersion: v1
kind: Service
metadata:
  name: web-svc
  namespace: shulie-takin
spec:
#  type: NodePort
  selector:
    app: web
  ports:
    - name: web
      port: 10008
      targetPort: 10008